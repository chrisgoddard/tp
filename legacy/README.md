# Legacy Fish implementation

This directory contains the v0.1 Fish plugin and its parity tests. It is
frozen; new development belongs in the standalone Bun implementation at the
repository root.

The directory is retained while v0.2 ships and will be removed after the v0.2
release.
